{
  "version": 2,
  "builds": [
    {
      "src": "src/web/server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["src/web/public/**"]
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "src/web/server.ts" }
  ]
}
